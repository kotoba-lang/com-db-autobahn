{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([db_autobahn.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'db_autobahn.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
